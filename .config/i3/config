# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!
#
# This config file uses keycodes (bindsym) and was written for the QWERTY
# layout.
#
# To get a config file with the same key positions, but for your current
# layout, use the i3-config-wizard
#

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:System San Francisco 0
# class                 border  backgr. text    indicator child_border
#client.focused          #36000A #840018 #ffffff #2e9ef4   #285577
#client.focused_inactive #840018 #36000A #ffffff #484e50   #5f676a
client.focused          #363636 #898470 #ffffff #2e9ef4   #285577
client.focused_inactive #898470 #363636 #ffffff #484e50   #5f676a
client.unfocused        #363636 #363636 #888888 #292d2e   #424242
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c
client.background       #ffffff

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

gaps inner 3
gaps outer -3
smart_gaps on

# use these keys for focus, movement, and resize directions when reaching for
# the arrows is not convenient
set $up k
set $down l
set $left j
set $right semicolon

# use Mouse+Mod4 to drag floating windows to their wanted position
floating_modifier Mod4

bindsym --whole-window Mod4+button2 exec --no-startup-id window_boxer.sh

# start a terminal
#bindsym Mod4+Return exec tilix
#bindsym Mod4+Shift+Return exec tilix --quake
#bindsym Mod4+Return exec urxvtc -fn "xft:Hack Nerd Font Mono:pixelsize:8"
#bindsym Mod4+Return exec urxvtc
bindsym Mod4+Return exec --no-startup-id kitty
bindsym Mod4+r exec --no-startup-id kitty -e ranger

bindsym Mod4+Mod1+x exec urxvtc -e nvim /home/kyran/.config/X11/Xresources
bindsym Mod4+Mod1+v exec urxvtc -e nvim /home/kyran/.config/.vimrc

bindsym Mod4+Shift+P exec firefox -private-window

# kill focused window
bindsym Mod4+Shift+q kill

# lock screen
#bindsym Mod4+Escape exec i3lock -i ${HOME}/Wallpapers/wallpaper-tile.png -t

# start dmenu (a program launcher)
#bindsym Mod4+d exec dmenu_run

bindsym Mod4+d exec rofi -matching fuzzy -show drun -p "Launch"

set $super_down exec "i3_helper_cmd.py super_down"
set $super_up exec "i3_helper_cmd.py super_up"
bindcode 133 $super_down
bindcode 133 --release $super_up 

bindsym Mod4+p exec "which rofi_menu.sh | xargs echo | sed -e 's/:/\n/g' | rofi -dmenu"

#bindcode 133 exec i3-msg border pixel 2
#bindcode 133 --release exec i3-msg border pixel 1
#bindcode 133 client.focused #FFFFFF
#bindcode 133 --release client.focused #444444
#bindcode 133 exec ${HOME}/bin/i3_timer super down; client.focused #FFFFFF
#bindcode 133 --release client.focused #444444; exec ${HOME}/bin/rofi_menu

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym Mod4+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym Mod4+$left focus left
bindsym Mod4+$down focus down
bindsym Mod4+$up focus up
bindsym Mod4+$right focus right
#bindsym Mod4+$left exec i3-msg border pixel 1; focus left; exec i3-msg border pixel 20
#bindsym Mod4+$left --release exec i3-msg border pixel 1
#bindsym Mod4+$down exec i3-msg border pixel 1;focus down; exec i3-msg border pixel 20
#bindsym Mod4+$down --release exec i3-msg border pixel 1
#bindsym Mod4+$up exec i3-msg border pixel 1; focus up; exec i3-msg border pixel 20
#bindsym Mod4+$up --release exec i3-msg border pixel 1
#bindsym Mod4+$right exec i3-msg border pixel 1; focus right; exec i3-msg border pixel 20
#bindsym Mod4+$right --release exec i3-msg border pixel 1

# alternatively, you can use the cursor keys:
bindsym Mod4+Left focus left
bindsym Mod4+Down focus down
bindsym Mod4+Up focus up
bindsym Mod4+Right focus right

# move focused window
bindsym Mod4+Shift+$left  move left  30
bindsym Mod4+Shift+$down  move down  30
bindsym Mod4+Shift+$up    move up    30
bindsym Mod4+Shift+$right move right 30
# precise moving
bindsym Mod4+Ctrl+Shift+$left  move left  5
bindsym Mod4+Ctrl+Shift+$down  move down  5
bindsym Mod4+Ctrl+Shift+$up    move up    5
bindsym Mod4+Ctrl+Shift+$right move right 5

# alternatively, you can use the cursor keys:
bindsym Mod4+Shift+Left move left
bindsym Mod4+Shift+Down move down
bindsym Mod4+Shift+Up move up
bindsym Mod4+Shift+Right move right

bindsym Mod4+i gaps inner all plus 5
bindsym Mod4+u gaps inner all minus 5

# split in horizontal orientation
bindsym Mod4+h split h

# split in vertical orientation
bindsym Mod4+v split v

# enter fullscreen mode for the focused container
bindsym Mod4+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym Mod4+s layout stacking
#bindsym Mod4+w layout tabbed
bindsym Mod4+w layout toggle tabbed split

# toggle tiling / floating
bindsym Mod4+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym Mod4+space focus mode_toggle

# focus the parent container
bindsym Mod4+a focus parent

# focus the child container
bindsym Mod4+z focus child

# move the currently focused window to the scratchpad
bindsym Mod4+Shift+minus move scratchpad

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
bindsym Mod4+minus scratchpad show
bindsym Mod4+Shift+plus sticky toggle

# switch to workspace
bindsym Mod4+1 workspace 1
bindsym Mod4+2 workspace 2
bindsym Mod4+3 workspace 3
bindsym Mod4+4 workspace 4
bindsym Mod4+5 workspace 5
bindsym Mod4+6 workspace 6
bindsym Mod4+7 workspace 7
bindsym Mod4+8 workspace 8
bindsym Mod4+9 workspace 9
bindsym Mod4+0 workspace 10

# move focused container to workspace
bindsym Mod4+Shift+1 move container to workspace 1
bindsym Mod4+Shift+2 move container to workspace 2
bindsym Mod4+Shift+3 move container to workspace 3
bindsym Mod4+Shift+4 move container to workspace 4
bindsym Mod4+Shift+5 move container to workspace 5
bindsym Mod4+Shift+6 move container to workspace 6
bindsym Mod4+Shift+7 move container to workspace 7
bindsym Mod4+Shift+8 move container to workspace 8
bindsym Mod4+Shift+9 move container to workspace 9
bindsym Mod4+Shift+0 move container to workspace 10

# Tab through workspaces
bindsym Mod4+Tab workspace next
bindsym Mod4+Shift+Tab workspace prev 

bindsym Mod4+grave workspace back_and_forth
# reload the configuration file
bindsym Mod4+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3https://www.youtube.com/watch?v=Jvmoux8BnSY)
bindsym Mod4+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym Mod4+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym $left       resize shrink width 10 px or 10 ppt
        bindsym $down       resize grow height 10 px or 10 ppt
        bindsym $up         resize shrink height 10 px or 10 ppt
        bindsym $right      resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left        resize shrink width 10 px or 10 ppt
        bindsym Down        resize grow height 10 px or 10 ppt
        bindsym Up          resize shrink height 10 px or 10 ppt
        bindsym Right       resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

#bindsym Mod4+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
        #status_command i3status -c ~/.config/i3/i3status.conf
#}


bindsym XF86AudioRaiseVolume exec amixer -q set Master 5%+ unmute
bindsym XF86AudioLowerVolume exec amixer -q set Master 5%- unmute
bindsym XF86AudioMute exec amixer -q set Master toggle

bindsym Mod4+Mod1+$Up    resize shrink height 30 px or 3 ppt 
bindsym Mod4+Mod1+$Right resize grow   width  30 px or 3 ppt
bindsym Mod4+Mod1+$Down  resize grow   height 30 px or 3 ppt
bindsym Mod4+Mod1+$Left  resize shrink width  30 px or 3 ppt

bindsym Mod4+Ctrl+Mod1+$Up    resize shrink height 5 px or 1 ppt
bindsym Mod4+Ctrl+Mod1+$Right resize grow   width  5 px or 1 ppt
bindsym Mod4+Ctrl+Mod1+$Down  resize grow   height 5 px or 1 ppt
bindsym Mod4+Ctrl+Mod1+$Left  resize shrink width  5 px or 1 ppt

default_border pixel 0
default_floating_border pixel 0
for_window [class="^.*"] pixel 0
#new_window pixel 0
# bindsym Mod4+x border none

exec --no-startup-id xrdb -merge ~/.config/X11/Xresources

# adjust key delay and repeat frequency
exec_always --no-startup-id xset r rate 350 45

exec_always --no-startup-id urxvtd -q -o -f

exec_always --no-startup-id picom -b --config ~/.config/picom/picom.conf &
exec_always --no-startup-id flashfocus
exec_always --no-startup-id ~/bin/i3_helper_common.py
exec_always --no-startup-id ~/bin/i3_helper.py &

exec --no-startup-id twmnd
#exec_always --no-startup-id feh --bg-fill /home/kyran/wallpapers/heping-island-park.jpg
#exec --no-startup-id pulseaudio --start
exec_always --no-startup-id nm-applet
#exec_always --no-startup-id compton --config .config/compton.conf
exec --no-startup-id /usr/lib/polkit-1/polkitd --no-debug
exec --no-startup-id lxsession &
exec --no-startup-id /usr/lib/poolkit-gnome/polkit-gnome-authentication-agent-1 &
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
